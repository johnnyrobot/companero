{
  "name": "companero",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test",
    "icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "sharp": "^0.33.4"
  }
}
